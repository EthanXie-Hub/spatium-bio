# `_drafts/`

Unpublished drafts. Anything in here is a starting point, not a
finished note — most likely written quickly or by tooling and waiting
for me to rewrite it in my own voice before it counts as a real entry
in `docs/notes/`.

Don't cite these as the project's build log. The published log lives
one directory up.
